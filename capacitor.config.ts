import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nyc.devs.app',
  appName: 'AsistApp2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
